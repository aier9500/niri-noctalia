# Niri — per-machine configs, scaffolded from niri/templates/*
niri/user/*

# Personal dictionary, stays local, never commited
voxtype/dictionary.txt

# Noctalia writes niri colours here on every theme change
niri/noctalia.kdl
