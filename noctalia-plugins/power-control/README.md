# Power Control

- dGPU sleep state.
- `cardwire` GPU mode switching.
- `asusctl` battery charge limits.

Partial parity the DMS "dGPU Sleep Monitor" and "Dank ASUS Control Center" extensions.

Requires `cardwire` (GPU mode daemon) and `asusctl` (ASUS laptops).
