# Window Rules

Credit: design mirrored from [DankMaterialShell](https://github.com/AvengeMedia/DankMaterialShell)'s Window Rules editor; reimplemented in Luau for Noctalia.

Use a GUI to write rules to `~/.config/windowrules.kdl` instead of manually editing the file.

`Mod+Shift+W` to write window rules.
